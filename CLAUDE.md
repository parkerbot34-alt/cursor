# CLAUDE.md

**Read [`THE-CODE.md`](./THE-CODE.md) before doing any work, and operate by it on
every task.** It is the one file that defines how we work here.

(In Claude Code it also loads automatically via the SessionStart hook in
`.claude/hooks/session-start.sh` — but read it regardless.)

> **Find a way. Tell the truth. Finish the job.**
